#include<bits/stdc++.h>
#define ASSAF ios::sync_with_stdio(0),cin.tie(0),cout.tie(0)
using namespace std;

// assaf
//  /﹋\
// (҂`_´)
// <,︻╦╤─ - - - -  accepted
// /﹋\
↓↓↓ START FROM HERE ↓↓↓

int main(){
    ASSAF;
    int t ;
    cin >> t;
    while ( t-- )
    {
        int x , y ;
        cin >> x >> y ;
        int ans = x^y ;
        for ( int i = 0 ; i <= 30 ; i++ ){
            if ( ((1<<i)& ans) != 0 ){
                cout << (1<<i) << "\n" ;
                break;
            }
        }
    }
    return 0;
}