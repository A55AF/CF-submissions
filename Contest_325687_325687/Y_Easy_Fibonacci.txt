#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    int a[100] = { 0 , 1 };
    for ( int i = 2 ; i < 100 ; ++i ){
        a[i] = a[i-1] + a[i-2];
    }
    cin >> n ;
    int res = 0;
    for ( int x = 0 ; x < n ; ++x ){
        cout << a[x] << " ";
    }
	return 0;
}
