#include<bits/stdc++.h>
using namespace std;
int main(){
  int a,b,ax,bx;
    cin>>a>>b;
    if ( a >= b ){
      for ( int i = b ; i>=1 ; --i){
        if ( a%i == 0 && b%i == 0 ){
          cout<<i;
          return 0;
        }
      }
    }
    if ( b >= a ){
      for ( int i = a ; i>=1 ; --i){
        if ( a%i == 0 && b%i == 0 ){
          cout<<i;
          return 0;
        }
      }
    }
return 0;
}
