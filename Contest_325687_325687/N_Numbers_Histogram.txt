#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
	char s;
	int n;
	cin >> s >> n;
	for ( int i = 0 ; i < n ; ++i ){
		int x[n];
		cin >> x[i];
		for ( int j = 1 ; j <= x[i] ; ++j ){
			cout<<s;
		}
		cout<<endl;
	}
	return 0;
}