#include <bits/stdc++.h>
using namespace std;

int main()
{
    double a , b , c ;
    cin >> a >> b >> c ;
    double result = ((2*a) + (3*b)) * (5*c);
    cout << fixed << setprecision(6) << result ;
    return 0;
}